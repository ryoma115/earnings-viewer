# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクト概要

日本の新規上場企業の決算資料（決算短信・有価証券報告書等）を取得・表示する Web アプリケーション。

## 現状

このリポジトリは初期化直後で、まだコード・依存関係定義・README が存在しない（コミット 0 件）。技術スタック、ディレクトリ構成、データ取得元、ビルド/テスト/起動コマンドはいずれも未定。

そのため、本ファイルにはまだ以下を記載していない:

- ビルド / lint / テスト / 起動コマンド
- アーキテクチャ・ディレクトリ構成
- 採用フレームワーク / ライブラリ
- データソース（EDINET, TDnet, 取引所サイト等）の選定

## 後続セッションへの指示

実装が進み以下のいずれかが追加されたタイミングで、本ファイルを再生成または追記すること:

- `package.json` / `pyproject.toml` / `Gemfile` などの依存関係定義
- 最初のソースコード（フレームワーク選定が判明する）
- データ取得先 API / スクレイピング対象の確定
- README.md の追加

特に、新規上場企業リストや決算資料の取得元（例: EDINET API、TDnet、JPX 適時開示、取引所の新規上場情報ページ）が決まった時点で、その API 仕様・レート制限・認証要否を本ファイルに記録することが、後続作業の生産性に直結する。
